{% extends 'base.html.twig' %}

{% block title 'Clampophilie' %}

{% block body %}
<div class="bg-light text-center">
    <h1>Clampophilie</h1>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit.
        Atque cum cupiditate delectus deleniti deserunt dolores,
        earum eius eligendi enim id libero minus odit quasi tempore totam, ullam ut vero voluptas!</p>
</div>
    <div class="container">
        <h2>Les derniers articles </h2>
        <div class="row flex">
            {% for article in articles %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('article_show', {
                                    id : article.id, slug : article.slug
                                }) }}">{{ article.titre }}</a>
                            </h5>
                            <p class="card-text">{{ article.auteur }} ({{ article.origine }})</p>
                            <div class="text-primary" style="font-weight: bold; font-size: 2rem;"> {{ article.prix }} € </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}